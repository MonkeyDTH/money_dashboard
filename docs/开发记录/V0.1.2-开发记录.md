# V0.1.2 开发记录

**开发周期**: 2026-05-25 ~ 2026-05-25  
**文档创建时间**: 2026-05-25 15:22  
**作者**: Leili + Claude Code

---

## 1. 版本目标

从飞书多维表格（Base）读取历史薪资数据，一次性导入本地数据库，完成薪资历史存量数据的录入。

## 2. 主要变更

- 新增飞书 Base 薪资导入脚本，从指定 Base 读取 91 条历史薪资记录并写入本地 SQLite
- 自动创建三家公司记录（爱奇艺 / 阿里 / 友塔）并关联就职时间区间
- 按字段语义拆分为 427 条 SalaryItem 明细（合同工资、奖金、补贴、五险一金、个税、公积金充值）
- 脚本设计为幂等，按 (company_id, period, pay_date) 去重，重复执行安全

## 3. 设计决策记录

### 3.1 公积金充值的方向归属

飞书表格中"公积金充值"为正数（如 2996、4340），而"五险一金个人"为负数。数学验证：

```
实际到手 = 合同工资 + 奖金 + 补贴 + 五险一金个人 + 个税扣除
```

公积金充值不参与上述等式，说明它记录的是每月公积金账户入账总额（含单位部分），而非从工资扣除项。因此存为 `housing_fund + direction=income`，独立追踪公积金账户累积，不影响应发/实发计算。

### 3.2 飞书 Base 授权域

飞书 Base 操作需要独立的 `base` domain 授权（`lark-cli auth login --domain base`），与 IM / Drive 授权相互独立。首次使用前必须完成此步骤。

### 3.3 period vs pay_date

模型中 `period` 为工资归属月末，`pay_date` 为实际发薪日。爱奇艺/阿里记录的日期本身即月末，友塔为每月 15 日，十三薪等特殊款项发薪日不固定——统一以飞书原始日期作为 `pay_date`，再推算对应月末作为 `period`。

## 4. 遇到的问题及解决

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 首次执行 `+field-list` 返回 `need_user_authorization` | Base 访问需要独立授权，user token 未包含 `base:field:read` scope | 执行 `lark-cli auth login --domain base` 完成浏览器授权后重试 |

## 5. 未完成事宜

无。

---

*本次开发由 Claude Code 辅助完成，人工审核所有代码变更。*
